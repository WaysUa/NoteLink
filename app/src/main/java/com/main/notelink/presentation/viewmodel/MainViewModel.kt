package com.main.notelink.presentation.viewmodel

import android.util.Log
import androidx.lifecycle.ViewModel

class MainViewModel : ViewModel() {
    fun init() {
        Log.d("MyLog", "MainViewModel: init main view model")
    }
}